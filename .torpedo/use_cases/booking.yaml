version: torpedo.darksub.io/v1.0
kind: useCase
spec:
  name: "BookingFly"
  description: "Fly reservation use case"
  doc: booking_fly.md # file path is relative to project
  domain:
    entities:
      - user.yaml
      - trip.yaml
